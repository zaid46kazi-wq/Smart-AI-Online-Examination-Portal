version: '3.8'

services:
  proctoring-ui:
    build: .
    ports:
      - "3001:80"
    volumes:
      - ./dist:/usr/share/nginx/html
    environment:
      - NODE_ENV=production
    restart: always
